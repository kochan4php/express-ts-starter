apiVersion: v1
kind: Service
metadata:
  name: express-ts-starter-svc
  labels:
    app: express-ts-starter
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
  selector:
    app: express-ts-starter
